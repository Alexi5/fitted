// import sharp from 'sharp';

// export const resizePhoto = (photo) => { 
//   return sharp(photo.imgUrl).resize(320, 240);
// };

// export const resizePhotos = (photos) => {
// 	return photos.map(photo => resizePhoto(photo))
// }

// // export const convertPhotoUrl = (photo) => {
// //   photo.imgUrl = `file:///Users/alexjennings/Desktop/fitted/public/photos/${photo.photoId}.jpg`;
// //   return photo;
// // };

// // export const convertPhotos = (photos) => {
// // 	return photos.map(photo => convertPhotoUrl(photo))
// // }